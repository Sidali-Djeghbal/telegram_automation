services:
  - type: web
    name: rss-telegram-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn bot:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 120"
